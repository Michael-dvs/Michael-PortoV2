# GEMINI API Key (required for Chrome extension or AI features)
GEMINI_API_KEY=""

# Application Deployment URL
APP_URL=""

# Supabase configuration (Vite client needs the VITE_ prefix to expose them to the browser bundle)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Database connection URL (for server-side / Prisma commands)
DATABASE_URL=""
DIRECT_URL=""

# Supabase Secret Admin Key (Server-side/migration usage only - do not expose to browser)
SUPABASE_SECRET_KEY=""
